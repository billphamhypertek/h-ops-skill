---
description: Unified DevOps toolkit for your server fleet (connect, overview, health, run, audit, deploy)
---

Invoke the **h-ops** skill to handle this fleet DevOps request.

Arguments: `$ARGUMENTS`

Route per the skill's SKILL.md (sub-commands: `connect`, `overview`, `health`, `run`, `audit`,
`deploy`). Read `~/.claude/skills/h-ops/inventory.yml` first to resolve server/group names, and
follow the skill's safety rules. With no arguments, print the usage summary and the list of known
servers.
